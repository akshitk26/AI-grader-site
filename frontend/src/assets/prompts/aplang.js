const argument = `Provide one argument essay prompt for AP English Language and COmposition. 
Make sure the topic of the prompt is always random.`;

const prompts = {
    'argument': argument
}

export default prompts;